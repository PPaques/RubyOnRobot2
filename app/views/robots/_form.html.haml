= simple_form_for(@robot, :defaults => { :input_html => { :class => 'input-xxlarge' } }) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name

  .form-inputs
    = f.input :fpga_adress

  .form-inputs
    = f.input :actif

  .form-actions
    = f.button :submit, :class => "btn-primary"

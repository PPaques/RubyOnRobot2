.page-header
  = link_to gpios_path, :class => 'btn' do
    %i.icon-list-alt
    Back
  = link_to @gpio, :class => 'btn btn-primary' do
    %i.icon-info-sign.icon-white
    Show
  %h1 Editing gpio

= render 'form'
